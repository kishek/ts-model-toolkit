/**
 * This is a test interface which acts as a super parent (parent of parent) interface.
 */
export interface TestInterfaceSuperParent {
  /**
   * A string property.
   */
  mySuperParentProperty: string;
}
