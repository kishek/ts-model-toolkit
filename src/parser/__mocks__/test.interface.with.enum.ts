import { TestEnum } from './test.enum';

/**
 * This is a test interface with an enum property.
 */
export interface TestInterfaceWithEnumProperty {
  /**
   * A enum property.
   */
  myEnumProperty: TestEnum;
}
