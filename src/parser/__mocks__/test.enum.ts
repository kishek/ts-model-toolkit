/**
 * This is a test enum.
 */
export enum TestEnum {
  /**
   * A test enum key/value pair.
   */
  ENUM_KEY_STRING = 'enumValue',
  /**
   * Another test enum key/value pair
   */
  ENUM_KEY_NUMBER = 5,
}
